# design_docs Index

**Paredros package, 2026-09-13:** its native host is now `paredros-client`;
historical `paredros-room` receipts retain their original name.

**Location, 2026-09-09:** Mesocosm's product document catalogue within the
[Isometry wing index](../../design_docs/DOC_README.md). Shared wing documents
remain here, once, and all three products are now in the same Git repository.

Canonical index for `design_docs/`. Per DOC_POLICY §5, this file wins over
any other index and is updated in the same session as any doc change.

## Working principles for AI assistants

- Read `../CLAUDE.md` first for repo role, terminology, and don'ts.
- Verify claims against the codebase and the sibling repos, not doc-to-doc
  consistency. This wing's founding record was corrected once already for
  trusting a stale index line over the plan it indexed.
- Plans carry done-conditions, not time estimates.
- The wing founding record's **Game tastes, integrated 2026-09-09** section
  owns the shared influence/preferences record. Preserve the distinction
  between endorsed directions, exploratory mechanics, and verified features;
  each vessel interprets the tastes through its own rules and care granularity.
- `PROJECT_DESCRIPTION.md` is maintainer-owned; surface contradictions, do
  not edit unasked.
- The substrate/system split is load-bearing across the whole wing: one
  substrate, many rule-dressings. Keep it that way in every doc.
- The three pipeline laws (choices-not-morphology, pointable inheritance, no
  homework) govern anything that crosses between games. A violation is a
  design bug, not a preference.
- Reproduction is the individual checkpoint; the epoch boundary is the
  lineage checkpoint. Do not make either one a hidden invocation of the other.
- Trophic warnings, unlock evidence, and epoch explanations derive from the
  same accepted world transitions as the ecology. A projection may summarize
  authority; it may not grow a parallel counter-based answer.
- Materialized individuals and far cohorts are two execution/storage forms of
  one world authority. Their explicit transitions preserve conserved totals,
  sufficient reading state, and every identity the game has made pointable;
  supported far-tier evaluators declare exact invariants and comparison
  envelopes against an all-individual reference; macro trophic views are
  derived rather than a third simulation.
- For visual anatomy, verify developed geometry and the captured silhouette:
  disjoint part envelopes can still merge from the game's camera.
- A voxel material is a compact id into saved world-local definitions. Dynamic
  scalar fields remain separately admitted planes, and each field names its
  consumer, honest dimension, cadence, sources/sinks, conservation rule, and
  scale reduction before the resident machinery carries it.
- Conatus schedules spatial systems and owns its resident allocations and
  leases. The host owns shared-device lifecycle, inter-tenant submission order,
  read epochs, completion, presentation, and recovery.

## Active docs

The wing founding record's **2026-09-05 historical-entry direction** allows
each vessel at any supported historical point, with characteristic eras rather
than stage locks. Its accompanying proposal distinguishes biological, cultural,
individual, and material inheritance, and branches past entry without rewriting
played futures. These are design directions, not completed history-entry APIs.

Its [2026-09-09 game tastes](2026-07-30_games_wing_founding.md#game-tastes-integrated-2026-09-09)
integrate Mark's supplied list and follow-up: skill with controllable chance,
ordinary-item development, procedural attachment, traversal, culture and world
memory, FX-first audio, co-op in every vessel, and control over portable data.
Ordinary items developing into personally significant equipment is explicitly
endorsed; rarity-gated development is not the baseline. Co-op's former blanket
deferral is superseded as design priority, with implementation gates kept local.

| Doc | What it is |
| --- | ---------- |
| [DOC_POLICY.md](DOC_POLICY.md) | Documentation governance |
| [PROJECT_DESCRIPTION.md](PROJECT_DESCRIPTION.md) | Product goals and pillars (maintainer-owned): the mesocosm names the enclosure scale, whose roster can range from one-trait cells through complex and fantastical organisms. |
| [2026-07-30_games_wing_founding.md](2026-07-30_games_wing_founding.md) | **Wing-level, cited by the sibling games.** The three vessels and their care granularities (species / individuals / community), **the invariant being care granularity rather than person purity** with its three replacement guardrails and the design space that relaxation opens, the lifecycle and the restated anti-Spore law, the three pipeline laws, worlds-as-moots with fili/tulpa/gemot mapping and graduated interop, layered critter/character/place/faction profiles carried as codicils (engrams at the time of writing), signed multi-writer facts with per-domain materializers rather than a universal CRDT, the MPL/permissive-library/CC-assets license split, the verified stack, the two-game proof pair, Voxatron as the material-language lodestar, and the ruling that anatomy topology is portable identity while capability remains vessel-owned. **Amended 2026-08-10 (R4):** each vessel is a mode of the same peopled history — Isometry the fortress and atlas, Paredros the adventure, Mesocosm the ecology beneath both — with the DF limit binding (shared history, never a shared running world), facts crossing by the pipeline laws while verbs never cross, platform organs going up the stack rather than sideways into wing crates, and identity shared as one vocabulary while the consequence grammar stays sovereign per vessel. **Amended 2026-09-02:** the memorial organ named "tulpa" here is renamed **hagiograph**; "tulpa" is reassigned to gemot's federated adapter-training lane. The fili/tulpa/gemot mapping above predates the rename; see repo `CLAUDE.md`. |
| [2026-07-30_mesocosm_founding_plan.md](2026-07-30_mesocosm_founding_plan.md) | Vessel 1's own design and phases: metabolize, incorporation-only parts with provenance, trait count as the real axis, an Exocosm-informed world-condition grammar including proto/soup and impossible worlds, the three trophic strategies, metabolic costs and material flow, and **the epoch loop** with complex lineages committing first and simpler lineages adapting later. Refreshed 2026-08-31 to clarify reproduction as the individual checkpoint inside that structure while the epoch boundary remains the lineage-scale review. Current integration order lives in the playable ecology plan. |
| [2026-07-30_engine_and_render_lane_landscape.md](2026-07-30_engine_and_render_lane_landscape.md) | **Historical candidate landscape**, last updated 2026-08-06. The verified pure-Rust engine field; renderling as lead mesh tenant; kiss3d as donor; the host-skeleton versus game-runtime distinction; V0-V2 projection proofs; `vello_hybrid`; capability profiles; and the tenancy cohesion contract. Its candidate labels do not override the later resident-views engine ruling or the 2026-08-18 critical review. |
| [2026-08-07_dependency_ledger.md](2026-08-07_dependency_ledger.md) | **Ordering authority; six cross-wing lanes proposed 2026-09-07:** creator, body interchange, voxel bodies/motion, game rules, world/history entry, and runtime/verification. Seeded body generation, creator previews and portable addressing begin together; world-scale work does not block local creation. Existing product chains remain: PE0-PE3 landed; PE4 and wider VB4 acceptance open; TG1 complete; typed tissue waits for TG2/TG3. Shared-engine consolidation is closed through D1, T1, V1b and `modulus`; product receipts and two-consumer promotion retain separate gates. |
| [2026-08-31_playable_ecology_plan.md](2026-08-31_playable_ecology_plan.md) | **Active integration and technical-architecture plan; PE0-PE3 fully landed, PE4 open, TG1 complete (2026-09-05).** Rules reproduction as the individual checkpoint and the epoch boundary as the lineage checkpoint; derives sparse causal history and bounded trophic-flow readings from one accepted transaction path; treats unlocks as evidence rather than a diet tree; preserves authored verbs under world-generated nouns, parameters, and combinations; records exact realized world rules; gives different forms different controllers over one world authority; and orders PE0-PE7 from the first useful ecology warning through succession, embodied discovery, lineage review, generated worlds, another form of life, cohort scale, and a collapse-and-recovery proof. Its downstream ownership map routes lossless individual/cohort transitions, compact material indirection, immutable body-volume mutation, revision-safe derived geometry, condition/development/process compilation, and consumer-specific fields into their owning plans without changing that order. **2026-09-02: §6 ruling 4 (material scheme), the affinity-table pack door, PE3's trait-array clause, and the founder-preview neighbourhood are ruled**; six of the original eight §6 rulings remain open. **2026-09-04: the last two of those are built, so PE3 has no residue left.** The trait array is deleted (five files, 1,318 lines; the authored pressures and world profiles kept as `mesocosm-core/src/pressure.rs` for PE4), and a founder preview now declares the poorest ground within the dispersal neighbourhood a birth can land in — the minimum column over the same square `ecology::bear` scatters into, off a shared `BIRTH_SCATTER` — so the quote is a ceiling a birth in reach can always meet, with equality pinned on uniform ground at tick zero. **2026-09-06:** reachable-tissue selection, checked body previews, Carry/Regrow confirmation, and the authored graft-practice scene are locally native- and replay-verified through the existing shared renderer. Stored-tissue inventory remains open; the 2026-09-09 clearing/cave/tunnel scene and paired reserve-return receipt live in phenotype section 8. |
| [2026-07-31_execution_waves_plan.md](2026-07-31_execution_waves_plan.md) | **Demoted to Wave 1/2 history** (audit 2026-08-07); superseded by the dependency ledger. Original description: the governing plans own the *what*, this owns the *when*. **Wave 1 complete**: `mesocosm-core` owning state while hosts only project, the Genet host, and the Isometry projection coupling by data rather than types. The Bevy host and the Renderling probe were **dropped** once the render lane was decided. Wave 2 in progress: 2.2 (epoch and ecology lab, three authored worlds) and 2.4 (the proof pair, Law C demonstrated in the consumer) are **complete**; 2.1 playfeel and 2.3 Paredros social proof remain. Carries the finding that the adaptation phase converges by round six with zero extinctions, because income is flat and uncontested. |
| [2026-07-31_phenotype_plan.md](2026-07-31_phenotype_plan.md) | **Mesocosm body decisions and proof plan; visible voxel integration added 2026-09-04.** Section 8 owns VB0-VB5: the live voxel-body draw path, procedural anatomy, addressed inspection, visible graft/expression and descendant changes, then roster/performance acceptance. Three source reviews complete; VB1 live route implemented and replay-verified locally, with pressure and headed lifecycle/fault acceptance still open. VB2 surface content, branching layouts, jointed appendage chains and foot/canopy spacing are locally replay-verified, with 538 passing tests and before/after captures; further anatomy refinement and recognition remain open. VB3 keyboard inspection is locally verified with 187 passing tests, addressed part highlights and history, and exact historical replay/PNG parity; pointer picking and player sensory cues remain open. TG1 now joins typed intake to inspection and revision-1 replay; VB4a has a locally verified authored family scene; the clearing/cave/tunnel playthrough now connects ordinary menus, a post-graft round trip and ecological offspring with exact replay. Its paired reserve-return experiment reproduces in both variants; TG2/TG3 follows. Allocation cells remain graph facts, distinct from render voxels. The body-volume boundary remains binding. Unifies the special played body with scalar organisms without requiring every distant life form to stay voxel-realized; separates structural dependency from cyclic functional links, somatic growth from filial adaptation, and anatomy from vessel capability; rules that adaptation previews a founder phenotype but commits a developmental program; **the trait array is deleted (ruled by Mark 2026-09-02, carried out 2026-09-04, §D4 condition 5), so §D4 is closed and P4 has landed**; puts burn-versus-incorporate first; and orders P0-P6. Whole-part loss remains the incumbent; sub-part voxel mutation must choose a new immutable content address or explicit body patch, atomically revise body facts, and feed revision-safe mesh/collider projections with a truthful lower-detail fallback. **2026-09-06:** the bounded graft menu/practice slice is locally verified at 960x540 and 1920x1080, with isolated voxel previews and exact Carry/Regrow replays. Full expression/descendant acceptance remains open. **2026-09-06:** ordinary-play expression selection and checked previews are locally verified with 857 passing tests, two ignored and 11 native runs, including a successful serial host retry after a parallel stall; somatic allocation and lineage programming remain separate. Allocation materials and descendant comparison followed in the next slice. **2026-09-07:** process-density materials and an explicitly assisted, counterfactual descendant comparison are locally verified with 952 passing tests, two ignored and 12 native runs. A snapshot-content bug in the comparison harness was fixed and all 96 host tests passed on retry. The same-enclosure join followed on 2026-09-08; unassisted-origin reproduction timing remains open. **2026-09-08:** authored VB4a family-practice joins part intake, grafting, expression and revision to an ordinary ecological birth in one enclosure, with an untreated living relative, generated content and exact replay. 108 tests passed, one ignored; a final focused inheritance guard and eight native runs passed. Natural encounters, unassisted-origin timing, typed composition and full-roster acceptance remain open. |
| [2026-08-01_epoch_boundary_plan.md](2026-08-01_epoch_boundary_plan.md) | **Design plan, partly built; refreshed 2026-08-31.** What happens *between* epochs. Reproduction is now explicitly the individual checkpoint while this boundary remains the lineage-and-ecology checkpoint; the exact epoch trigger is open. Speciation is an act rather than a threshold. Shared-lineage adaptation uses immutable revisions and adopt-or-branch authorship; significance is abnormality measured against the world's own record. The event log, species tree, world record, place graph, reckoning, and player speciation have landed; shared adaptation, NPC speciation, goals, plasticity, and the full played boundary remain. **The old `epoch.rs` trait array is deleted (ruled 2026-09-02, carried out 2026-09-04); the seven authored pressures and three world profiles survive it as `mesocosm-core/src/pressure.rs`.** |
| [2026-08-01_processdef_plan.md](2026-08-01_processdef_plan.md) | **Mesocosm's extensible biology lane; status refreshed 2026-09-01.** Defines `ProcessDef` as a namespaced transformation rather than a universal gene or ability flag; separates acquisition, developmental expression, embodied process allocation, channels, capability, and ecology. A displayed generated trait compiles into an event-driven condition program, a discrete development program, and/or a native repeated process, each typed and bounded with explicit stacking and delayed feedback. PD1a's transactional `BodyPhenotype` design and the live recipe-to-body constructor join are complete. **PD1b is complete**: its identity slice landed 2026-08-08 and its allocation half on 2026-09-01, giving every living part an authoritative cell-graph mosaic seeded from geometry and one validator for direct and automatic arrangement. **PD2 landed 2026-09-01** with one native played process, a gland. **PD3 landed 2026-09-01**: the definitions ship as a data-only JSON pack under `packs/mesocosm/`, admitted by the new MPL-2.0 `mesocosm-phenotype` crate, lowered to a ruleset a world now records by digest, and PD2's temporary editor operation is deleted in favour of a bounded `Intent::Express`. PD4’s Piccolo authoring parity is next. **The affinity-table pack door is ruled (Mark, 2026-09-02): a pack-declared affinity overrides `Founding`, which is the fallback.** |
| [2026-07-31_wing_phenotype_contract_plan.md](2026-07-31_wing_phenotype_contract_plan.md) | **Wing body identity and character creator scope; v0 format extraction and focused interchange proofs verified 2026-09-09 (section 12).** W0-W6 govern subject/revision/part identity, branch provenance, body/chronicle v1 and sovereign readings; v1 remains unimplemented. Section 11 adds C0-C5: seed-plus-criteria generation alongside authored fixtures, later detailed parts editing, optional description interpretation, system-faithful Isometry mapping, two-host inspection/checked preview, local creation, portable admission, body-in-use and historical starts, with primary-source research and current code/WIP boundaries. The first local Mesocosm slice joins seeded terrain/soil and constrained body generation through candidate reports and native entry; its section records verification and the merged graphical picker, including inert cancellation and exact recorded replay. The 2026-09-08 follow-up adds retained feeding role, movement organs and segment count, rerolling within those filters, and separate saveable criteria drafts. Its continuation adds seeded branching layouts, a body-plan selector and version-1 axial replay compatibility, followed by whole-body preview depth and an oblique overview. Held-body habitat comparison (2026-09-08) adds nutrient distribution, population, starting access and disposable observations under fixed biological rules; the 2026-09-09 continuation adds recorded intake/reserve/life-cycle evidence and recent same-body comparisons. The dependency ledger maps six cross-wing lanes; local creator work can precede interchange and world-scale work. |
| [2026-08-02_views_founding_plan.md](2026-08-02_views_founding_plan.md) | **Mesocosm's UI founding.** Adapter-first: `mesocosm-views` discloses world facts as sceno scores, scenomise solves, sprigging leaves realize; a layout computed in views is in the wrong crate. First chrome is the **minimap** (which lineage dominates each region), consuming `Arrangement::Hulls` whose cells match `Places::at` exactly (the congruence test). Region meaning stays per-vessel; the backdrop is a dynamically generated DOM layer. Hosts are **staged**: route A (textless HUD lane: leaf → paint_list_render → netrender on the shared device → overlay blend) landed 2026-08-02 with the minimap on screen (`14_minimap.png`), and shook a premultiply bug out of paint_list_render; route B (cambium lane) waits for the epoch-boundary screen, and the HUD never grows a text hack. The generated backdrop landed the same day (the world rendering its own enclosure top-down on a cadence, under the translucent cells; `15_backdrop.png`), surfacing the vello display-encoded-output seam (sRGB twin copy). Interactivity and fields-over-cells open. |
| [2026-08-05_place_graph_engine_plan.md](2026-08-05_place_graph_engine_plan.md) | **The active world-substrate record, clarified 2026-09-01.** Records the 2026-08-05/06 fundamentals rulings (one authority per capability; shared engine organs with sovereign verbs; volumetric truth permitted; verticality as a simulation affordance; distinct world graphs; elective continuous nesting; hybrid worldgen; per-vessel design reference targets; **three-tier physics**: integer authority / parry+owned kinematics advisor / GPU ambience, rapier in reserve; renderer tenancy per the cohesion contract; **Mesocosm camera pulled back Rain World-style 2026-08-06**, the brick tracer's primary consumer becoming Paredros). The model is a place graph indexing one continuous voxel space and brick truth per region. Near/Far currently selects detail while every organism remains individual and `Cohort` is a derived conservation projection; deterministic individual/cohort execution is the gated target, while macro trophic state remains derived and render residency cannot select it. G0-G4 are historical build labels rather than acceptance gates. P0-P8 compose generated Ground, player and autonomous crossing, replay, body projection, native/Browser WebGPU, netrender, and population cost in one burrow run. **P9-P10 are implemented locally 2026-08-21:** live body geometry constrains locomotion, tunnel growth, and terrain sight, producing compact/broad passage divergence and compact/tall perception divergence through the ordinary ecology policy. |
| [2026-08-06_general_model_plan.md](2026-08-06_general_model_plan.md) | One causal grammar and sovereign evaluators. Ecological E0-E4 slices landed; acceptance gates remain open. Sections 7.1-7.3 retain open discussion of interacting magic, anatomy, operators, vows and generators. Section 7.4 (2026-09-13) adopts a configurable glyph canon and journey-shaped divinity; G1/G2 shared kernel and optional accepted-event Mesocosm trial integration pass 16 kernel and 44 runtime tests. Full effect packs and durable divine gameplay remain G3/G4. **2026-09-15 rulings:** hagioglyph names the whole divinity organ (G5's time-varying glyph is now "the canon revision"); G6 chains and standing (typed chain admission, success from affinity minus resistance and chain length, failure raising a capped, decaying resistance that doubles as defense, affinity raising magnitude and side effects); G7 impresa, the association organ (one record type over any pointable identity, fact/belief/legend readings, world-configured kinds, consequential records exempt from compaction, a plain `wing-` crate beside wing-glyphs with products owning storage). Neither G6 nor G7 is started. |
| [2026-08-18_vessel_briefs_and_presentation.md](2026-08-18_vessel_briefs_and_presentation.md) | **The ratified vessel briefs and the camera rulings** (2026-08-18). Isometry: procedural voxel VTT, pluggable rulesets, PF2e/SRD campaign end-to-end as first release, Role/Creator modes. Paredros: voxel Kenshi, third-person continuous zoom, first person as a Paredros-only setting. Mesocosm: ecological roguelike, **terrarium section** + trait-graph review board; the oblique default remains while the 2026-09-05 accepted shallow-depth/quarter-turn direction awaits the clearing-and-burrow prototype. Shared-world fidelity = metadata broad strokes and motifs, never pixel consistency; the primordial-name hook; **arc / unprecedented event / epoch modifier as one record-first mechanism at three timescales** (the architected answer to Kenshi reactivity); alignment as a ruleset binding with commitments (edicts/anathema) as the arc primitive, axes-as-data per world. Closes the founding record dimensionality carry; corrects the CLAUDE.md identity line. |
| [2026-08-14_resident_views_composition_plan.md](2026-08-14_resident_views_composition_plan.md) | **How the voxel world composes with Burn/CubeCL, the tracer, collision, and admitted fields on one device.** Ratified 2026-08-14: voxel-authoritative and Burn-addressable; `ResidentChunk` as the zero-copy seam; **Burn proposes, the record disposes**. Carries the hybrid engine ruling: CubeCL-authored compute, brick raymarch ground, renderling mesh tenancy, netrender composition, Parry tactile projection, and runtime indexes rather than record authority. Lanes A-F, the seed crystal, real-Ground commit/replay, projection cache coherence, shared DDA ownership, and V1b incremental residency are complete with headed receipts. The 2026-09-01 boundary clarifies that resident planes are carriage rather than demand: every product field first names its authority class, consumer, honest dimension, cadence, dynamics, conservation, scale behavior, and cost. Field-plane visualization remains open. |
| [2026-08-28_played_slice_plan.md](2026-08-28_played_slice_plan.md) | **Active played host; refreshed 2026-08-31.** PS0 and PS1 landed mechanically and were playtested: the live `World` runs in the brick-traced side-on terrarium with trace-on-exit and replay-identical receipts. Vitals, the roster, framing, hunger-routed metabolize, terrarium dynamics, and default archetypes followed. PS2 is now decomposed by the playable ecology plan: PE1 owns the individual reproduction/succession checkpoint and PE3 owns the separate lineage-scale epoch review; PE0's first ecology reading precedes both. |
| [2026-08-18_engine_ecology_rulings_and_review.md](2026-08-18_engine_ecology_rulings_and_review.md) | **Engine review through 2026-08-26; cross-port sharing audit added 2026-09-09 in section 7.** The audit prioritizes shared wire formats and live consumer tests, body identity, dependency alignment, GPU readback/surface reuse, and minimap adoption; it distinguishes existing reuse and product-specific rules. Preserves record authority, one resident allocation path, hybrid raymarch-plus-renderling composition, Conatus-private tactile realization, and evolved controllers as bounded intent proposers. Corrects the overclaims: shared render chunks are narrowed rather than abolished; DDA traversal is reusable but renderer policy is vessel-owned; Mesocosm is cheaper than Paredros rather than nearly free; the GPU-only renderling bake seam includes allocation, lifetime, count, ABI, and draw receipts; nexus is ruled decomposed-into-Conatus (2026-08-28), gated on the first dynamic-body consumer. R1 traversal, R2 GPU mesh bake, R3 committed collision, B1 bounded fauna policy, V1 continuous-zoom residency, V1a projection identity, the brick platform ownership lift (now `modulus`), D1 raymarch/renderling depth composition, T1 terrarium picking through Conatus, and V1b's stable resident brick cache are complete. The exact-revision consumer suites and headed equal-extent rerun are green, with a byte-identical PNG after extraction. The ordered shared-engine chain is closed; remaining spatial contracts are consumer-forced. Clipmaps and compact procedural vertices remain consumer-gated. |
| [2026-08-29_terrarium_dynamics_plan.md](2026-08-29_terrarium_dynamics_plan.md) | **Landed and closed through TD11; status reconciled 2026-08-31.** The series answered the first playtests with canonical 10 t/s tempo, instincts under idleness, hunger-routed metabolize, visible vitals, a multi-body roster, one economy for all life, mid-life founders, a milligram-conserved soil/body/carrion cycle, anatomy-derived growth and upkeep, adult-mass reproduction, kin-aware appetite, body-derived sight, and gradient following. Its population instrument remains diagnostic, while `breathes` no longer spawns tuning rounds for systems not yet built. Product residues now belong to the playable ecology plan. |
| [2026-08-29_scale_plan.md](2026-08-29_scale_plan.md) | **Active scale ladder; S1 landed and status refreshed 2026-09-01.** Records the measured terrain, atlas, place-graph, far-tier, population, and snapshot limits. S1 widened the terrarium to 129 voxels with 916 founders and milligram conservation while holding the 10 t/s budget. S3 now separates exact individual/cohort transition fidelity from later execution fidelity: persistent identities and conserved state survive lossless zero-tick round trips, each far-tier reduction declares exact invariants and a comparison envelope against an all-individual reference, and unsupported behavior pins affected subjects to individual execution. The S2 windowed-residency versus S3 cohort-order decision remains open; S4 zoom and S5 stress follow. PE6 consumes the lane rather than making it the next product slice. |
| [2026-08-29_open_rulings_register.md](2026-08-29_open_rulings_register.md) | **Historical 2026-08-29 snapshot, partially superseded.** Preserves the 57 questions collected from the played slice, terrarium dynamics, scale, and three research briefs, but TD8-TD11 and DC1-DC4 later closed or reframed several entries. It is retained as a map to the source discussions rather than current execution order. Check the owning source plan and the playable ecology plan before acting. **§10 (the far-roster cap) is ruled 2026-09-02: silhouettes, landing with S4.** A **"2026-09-02 scoping" section** appends the deferred bucket and the stack-rounds list Mark set aside that day, verbatim, not to be re-raised until their own slice arrives. |
| [2026-08-29_elements_and_traits_memo.md](2026-08-29_elements_and_traits_memo.md) | **Memo; storage boundary refreshed 2026-09-01, material scheme ruled 2026-09-02.** Keeps the closed/open seam between authored verbs and generated nouns rather than between two trait authorities. Compares typed matter, coefficients, and exchange payloads; defines compact world-local material ids plus saved definitions; separates dynamic scalar planes from cell and body facts; prices fields by their actual dimension and cadence; and defines reachability, fingerprint, collision, and rank tests that prevent a large vocabulary from becoming decorative affixes. **Ruled 2026-09-02: matter is typed by provenance: storage is scheme A, payloads are scheme C, no fields in PE4's first world**; see the playable ecology plan §6 ruling 4. |
| [2026-08-29_traits_and_perception_brief.md](2026-08-29_traits_and_perception_brief.md) | **Design brief; acquisition execution refreshed 2026-09-01.** Organism-to-organism edges carry durable pair relations, and unlock conditions extend beyond a linear eating tree into stress, use, failure, environmental exposure, relationships, achievements, and donor-part evidence. Conditions run from relevant accepted events and deterministic bounded reducers, record exact evidence and candidate digests, and declare a cohort reduction or pin affected subjects to individual execution. The existing free `learn_from` path remains a half-circuit: it teaches every donor-recipe appendage after either meal route, changes complexity, and expresses no new body. Pricing, lineage distance, NPC acquisition, trait-relative perception, deception, and memory remain open. |
| [2026-08-29_forms_of_life_brief.md](2026-08-29_forms_of_life_brief.md) | **Research brief; playable animal-, plant-, fungal-, and microbial-scale roster reaffirmed 2026-08-31, stages still unruled.** Decomposes forms across energy source, matter acquisition, motility, scale relationship, host relationship, reproduction, and information. Mixotrophy remains the first blocked combination; a durable organism edge is required for attachment and symbiosis; parasite and mutualist are proposed as net-flow readings rather than classes. The playable ecology plan adds the required representation decision for connected versus multi-anchor subjects and proves one second controller before roster expansion. |
| [2026-08-30_default_creatures_plan.md](2026-08-30_default_creatures_plan.md) | **DC1-DC4 implemented; visual acceptance open, refreshed 2026-09-05.** Owns the authored archetypes, part economy, render budget and thirty-seed ecology diagnosis. Phenotype section 8 owns the live voxel-body integration. CP1 now supplies a native-verified clearing/burrow, four hash-neutral views, addressed inspection and ordinary traversal (377d774); terrain readability is native-verified, with canopy composition and articulated foot contact still open. The default ecology camera remains oblique pending visual review. Q10 diagnosed unrestricted predator intake and the 17/30 roster versus 1/30 baseline collapse; TG1 is complete: new collapse counts are 13/30 roster and 1/30 drawn, with no run retaining all three tiers. TG6 remains open; VB4a is the next visible body-change join. |
| [2026-09-01_dev_tools_plan.md](2026-09-01_dev_tools_plan.md) | **DT1-DT4 landed 2026-09-02; fixture defaults wired 2026-09-04.** Dev tools for sitting in a run and interrogating it. DT1 gives time under the hand: `--dev` adds a cambium dev lane (a `workbench` tile, not a hand-rolled corner) and five host-only keys — pause, step, step ten, speed down/up a five-rung ladder — that pace `Runtime::advance`/`::step` and never enter the trace, receipted with a `dev: bool` flag. DT2 adds follow and inspect: `N`/`B` cycle the camera through the living roster in id order and `M` snaps it back, moving the section's follow centre and nothing else, while the one dev tile reads the followed critter entirely from core queries — id, lineage, position, its reserve and substance accounts, its own flow accounts with their window, its line's revision, the world's discoveries, and its parts with role, half-extent and sites, truncated with a count. Two core readings were added for it (`flow::Accounts`, `History::ending`); click-to-select in the section was left out for want of picking machinery in the host. DT3 adds the other kind of dev action, the kind that changes the world: four ordinary `Intent`s — `EndEpoch` (`X`), `ForceBirth` (`F`), `Kill` (`K`) and `PlaceMatter` (`G`) — each queued through `Runtime::queue`, in the trace, refused by name, and conserving matter to the milligram. None has its own physics: ending runs PE3a's boundary, a forced birth is `ecology::bear` and a dev kill is `ecology::perish`, both shared with the tick's own passes, and placed matter enters through a named `Account::Dev` so the flow record still reconciles. `EpochRule::PlayerTriggered` is built by it; the receipt gained `dev_intents` and labels such a run **assisted**. **DT4 landed 2026-09-02 and completes the plan**: the bespoke replay-and-demo harness is folded into genet-probe, `mesocosm-genet` implements `Automatable` and `Driveable`, and `--scenario <path>` drives a run from a text file — `act` carrying the host's own documented key names through the one `press_key` route, plus five host-side names (`follow`, `follow-nearest`, `follow-child`, `hunt`, `demo`) for what no key says. `--record-demo`, `--auto-eat` and `examples/dt3_script.rs` are deleted and re-expressed as scenarios; `--replay` survives because nothing pumped inside an app can found the world it is running in. On the way it reconciled the epoch boundary's **two disagreeing doors into one**: `World::end_epoch` and `Runtime::end_epoch` are deleted, `World::apply`'s boundary block is the only door, and `World::reckon` stays the separate read-the-past call. Whole-ecology and run-comparison panes are out of scope until their home is ruled. **Fixture defaults are ruled (Mark, 2026-09-02) and built 2026-09-04: the headed binary's default trace/receipt/capture paths are `scratch_played.*` under the testing home, off a named `played::DEFAULT_STEM`, and the golden `ps1_played.*` fixture is written only when a flag names it — asserted by `defaults_do_not_name_the_golden_fixture` rather than kept by hand.** |
| [2026-09-04_trophic_grammar_plan.md](2026-09-04_trophic_grammar_plan.md) | **PE4's first build; accepted 2026-09-04, TG1 complete 2026-09-05.** Shared typed admission, inspector and revision-1 replay pass with 956 tests and native captures. Thirty seeds per arm yield 1/30 drawn and 13/30 roster collapse; neither arm retains all three tiers, so TG6 remains open. Phenotype section 8 joins its mechanics to visible voxel bodies; TG2a now has typed stock arithmetic, a scalar-preserving broken control, serialized receipt replay, and a measured four-channel transport kernel (2026-09-09). TG2b integrates live four-channel Soil with checked transfers, untyped-only roots and revision-2 snapshots. TG2c adds per-part scruple, checked typed attachment and preserved donor mixtures in graft/part transfers under grammar revision 3. TG2d retains mixtures through whole meals and NPC growth, records reserve digestion and keeps typed spills pending in soil under grammar revision 4. TG2e preserves birth provisioning and records completed upkeep, travel and decay returns under grammar revision 5. TG2f adds producer synthesis and declared founder tissue recipes under grammar revision 6 and creator request version 4 (2026-09-12), with 753 core/runtime tests passing. TG2g closes pending soil mineralization under grammar 7 (2026-09-13): 802 core/runtime tests pass and four 4,000-tick runs reconcile every channel. TG2h repeats the same 917-founder 20/200 window twice with per-tick wall/CPU attribution: all 1,200 measured ticks stay below 55 ms and retain exact baseline hashes and deterministic counters. TG2 conservation/replay and its defined 100 ms gate pass. Historical 175-263 ms outliers remain unattributed; no simulation optimization or lifetime timing guarantee is claimed. TG3a bounded disfavoured Carry compatibility is verified under grammar 8 (864 core/runtime/view tests and native all-features/all-targets check): Mark approved a configurable one-cell allowance and equal-mass reserve cost. Dietary inspection, learned compatibility acquisition and TG6 ecological viability remain open. The specimen bench owns dietary appearance integration. Turns the 2026-09-02 material ruling into a trophic grammar: intake as typed ports, so herbivore, carnivore and omnivore are readings rather than labels; typed stock in soil and bodies with per-channel conservation rewritten first; the two composition layers; plates gating bite and payloads firing on provenance at the meal; selective edibility by port match rather than a pairwise table; the roster re-declared and the thirty-seed corridor measured on both walls; and a display-only lexicon in the packs. |
| [2026-09-11_orthographic_voxel_presentation_plan.md](2026-09-11_orthographic_voxel_presentation_plan.md) | **Path C selected; yaw/variety probes, Bench A/B and bounded Idle Bench C complete (2026-09-13).** The standalone probe and compact receipts are retained under mere/crates/probes/wing-three-paths. Section supplies posed geometry bounds, preview framing and visible-part queries with headless pixel checks. The `--bench` entry embeds that shared-depth scene in Genet through Cambium's producer lifecycle, resolved CSS appearance and transformed native input. Fresh native captures cover the parts examiner, palette/decorative styles, opacity, zoom, overlay priority, resize, removal/recreation and selection expiry; exact occlusion and clip edges retain their focused fixtures. Generation comparison is native-verified. Generation wiring adds seeded parent trees, eight starting anatomies, seed/role controls, independent content size and paid mass, and exact resized specimen saves; native acceptance passes. Composable anatomy adds independent layout, organs and bounded counts; native acceptance and saved replay pass. A native-verified planar glyph-effect experiment separates guaranteed responses from seeded receiver rules, with animated marks, compatible strings and saved replay. Opaque spatial forms cover orbit, mesh-face inscription, tether and emission with shared body depth, seeded appearance and two orthographic views. A 21-configuration sparse population receipt measures body/design counts independently through 1000, including native CPU stages, uploads and foreground coverage; 23 dense/layered configurations add up to 95% foreground coverage and a separate completion diagnostic. Integrated real-scene budgets remain open. Species likeness is still a visual-quality gate. A bounded disposable idle world trial now reuses runtime checkpoints and accepted movement/feeding records; integrated population/cost measurement remains open. General CSS 3D, large-DOM scaling, planar retention, crate consolidation and portable body v1 remain separate. Soundscape and visual effects consume body/world activity. |
| [2026-09-14_isometer_extraction_plan.md](2026-09-14_isometer_extraction_plan.md) | **Assessment for the presentation plan's L9; no code moved (2026-09-14).** How `mesocosm-genet`'s `Section` (23 files, 4,569 lines, 34 `World` lines of which 15 are non-test) becomes the Isometry path crate `shared/isometer`, with Mesocosm's `Section` and Paredros's P1 producer as thin adapters. Inventories Section's whole public surface with its callers, maps every `World` read to a product-neutral input (body document, continuous-yaw pose, volume source, tint, subject plus part id, revisioned terrain source), and proposes the crate's API: `SceneBody`/`SceneFrame`, a `VolumeSource` with the `DeclaredExtentVolumes` declared-extent solid fallback Paredros anatomies need, a `TerrainSource` over the shared `mesocosm_core::places::Ground`, and one `SlabCamera` orthographic slab carrying a world-space `Cutaway::Bounds`/`Plane`, plus a `SceneProducer` holding the skip and sRGB/straight-alpha contract. Checks that lens, render, mesh, wgpu 30 and cambium-rootstock resolve identically for both workspaces and that nothing reaches `mesocosm_core::World`, `paredros_world` or `isometry_core`. Gives an eight-step move order that keeps both workspaces green, and names the open decision: `OrganismId` is u32 while Paredros's `SubjectId` is u64, so `LiveBodyProjector::project` cannot carry a scene identity unchanged. |
| [2026-09-15_isomere_plan.md](2026-09-15_isomere_plan.md) | **M0 to M5 landed 2026-09-15; M6 remains.** isomere, the wing's GUI layer: the panels, sheet tokens and host assembly the three products each built over Cambium, promoted once from the existing GUIs and consumed back, with a tinct-seeded palette that keeps today’s pixels. The shared sheet, the viewport card, the examiner, the journal, the status and help lines with a declared keymap, and the winit host assembly are in; Paredros runs on the assembly and its 3,020-line netrender body sheet is retired, net 2,610 lines removed. Two rulings of 2026-09-15: Mesocosm's main-binary host migration is deferred to a later round, because its chrome lays out in physical pixels against the shared host’s logical ones and the move rebases its captures however it is done; and the examiner’s schematic slot stays unfilled, since stroked paths in a scene are a rewrite in a document rather than a move. M6, the dev tile and the shared receipts panel, is the remainder. |
| [2026-09-14_isometer_family_plan.md](2026-09-14_isometer_family_plan.md) | **Assessment for the isometer family; no code moved (2026-09-14).** How `mesocosm-lens`, `mesocosm-render` and `mesocosm-mesh` stop being Mesocosm-owned path dependencies and become isometer's component crates, so all three games consume one neutral family. Maps the current graph (mesh below render, lens beside both, no component depending on isometer) with each consumer's actual imports, and inventories every mesocosm-core type the three name: `PartId`, `VolumeRef`, `Yaw`, `Part`, `BodyDocument`, `Provenance`, `Origin`, `Aabb`, `places::Ground`, `Role`/`classify` and the postcard/blake3 seam are product-neutral and land in a new `isometer-core`; `OrganismId`, `process::Process`, `Places` and `Rng` are not and are severed behind Mesocosm-owned adapters. Proposes one Cargo workspace under `shared/isometer` with the facade at its root and `isometer-{core,lens,render,mesh}` beneath, MPL-2.0 throughout, path-resolved per product (all four names free on crates.io, checked read-only). Resolves the presentation plan's open L2 decision: mesh is the merged appearance crate and `isometry-voxel` folds into it, neither the Isometry root workspace nor mere. Gives an eleven-step move order sequenced after the tracer floor fix, the mesquite/taproot pin bumps and Paredros's P1 retarget, each step leaving all three workspaces green, and names the blockers: state-hash and wire-byte stability across the core split, the root workspace's divergent mere pin (`fb7e136b` against the family's `4f4de1d0`) that keeps Isometry out, and `isometry-voxel`'s MIT/Apache licence against the family's MPL-2.0. |
| [2026-09-15_effect_pack_preset_plan.md](2026-09-15_effect_pack_preset_plan.md) | **Assessment for general model §7.4 gate G3; no code moved (2026-09-15).** How the bench's live trial markers stop being hardcoded strokes and colours and start reading the bound journey's grants through a default effect pack, as one replayable specimen-bench preset. Tabulates the two flows that run past each other today (accepted event → hardcoded marker; accepted event → grant → journey) and finds the break: `enable_glyphs`, `Trial::glyphs()` and `wing_glyphs` appear nowhere in `mesocosm-genet`, so G2's consumer is the runtime and the example, never the bench. Splits the pack Mark's way — the **declaration** (effect id, display, behaviour kind, receiver class, cost unit, explanation template) shared now in a new `wing-glyphs/src/pack.rs` with no execution, the **execution** table kept product-side in `mesocosm-core/src/effect_pack.rs` beside the effect experiment until a second product consumes one — with Rust signatures, a grant-to-marker mapping, and what "paints nothing until acquired" means for each of the four existing marker kinds. Picks the **acquiring act** (carved / fed / moved) as the journey-rule axis over the provenance-kind axis, which is degenerate in Mesocosm because every grant the adapter writes is `ProvenanceKind::Event`, and authors three rules on one glyph: carving-earned returns as a per-use surface inscription, feeding-earned as an emission that lasts only while feeding continues, movement-earned as a free path trail, each cited to its acquisition evidence. Amount enters from uptake milligrams, carve voxels and meal mass through a fixed integer curve, so two recipients differ for a stated reason and replay stays exact. Gives an eight-step move order gated on the trial and uptake-world scenarios, the 44 runtime tests and the 32-viewport spatial-coverage capture, and names the blockers: the `testing/glyphs/journey.json` baseline is measurably stale (`765c055b377dfb62` recorded, `df397e7c183eec55` today), §7.4's 16-test kernel receipt is now 19, `mesocosm-core` does not yet depend on wing-glyphs, and the G5 revision stamps mean a pack lookup must key on the current canon's effect while completion stays judged against the founding one. |
| [2026-09-15_glyph_expression_plan.md](2026-09-15_glyph_expression_plan.md) | **Assessment for general model §7.4, superseding the effect pack plan's acquiring-act axis; no code moved (2026-09-15).** How every base glyph in a canon comes to be expressed by one or more traits, and how experiencing a glyph is kept separate from embodying it, after Mark's ruling that experiencing the glyph is the main thing and that a glyph is had rather than performed. Finds that Mesocosm has no `Trait` type and that the only rule-bearing, part-borne datum is `phenotype::mosaic::Site` — an admitted `ProcessRef` occupying named cells of a named living part — placing `Appendage` (a lineage lexicon word), `Role` (recomputed geometry), `FaunaTraits` (a derived per-tick reading), `guise` (a claim the ecology may contradict) and `scruple` (what a part is made of) as the near misses. Proposes a per-canon-revision expression table shared in a new `wing-glyphs/src/expression.rs`, keyed by an opaque `namespace:local` trait id, because `ProcessId::qualified()` already emits exactly the shape wing-glyphs' `identifier()` validates and the sibling games need a key they can read; `mesocosm-core` keeps only the resolution and a pure `embodied(&BodyPhenotype, &Registry, &ExpressionTable)` reading over living attached parts, so Grew, Grafted, Severed and Died change what a body embodies with no stored field. Sets out the experience model as journey-side acquisition under `ProvenanceKind::Trait` — a variant that has existed since G1 and has never been written — triggered by three declared conditions (borne, used, befallen) modelled on `discovery::Condition`, and distinguishes experiencing all (ascension) from embodying a required subset (mantling). The bench emits an embodied glyph's marks from the bearing part's own face through isometer's existing `glyph_anchors`, shows an experienced-but-not-embodied glyph unanchored, and keeps the read-only direction. Gives a file-by-file keep/rework/discard of the uncommitted work and an eight-step move order gated on the four trial scenarios, the 32-viewport spatial-coverage capture, the six move pins, the 24 wing-glyphs tests and the 49 runtime tests. Names the blockers: five native process definitions against a §7.4 default canon of letters, numbers and symbols; whether the expression table is seeded and shuffled per world like the canon correspondence or authored; and what a critter with no expressing trait can ever do. |
| [2026-09-15_trait_catalogue_plan.md](2026-09-15_trait_catalogue_plan.md) | **Assessment founding the trait catalogue the glyph expression plan consumes; no code moved (2026-09-15).** Sizes and structures the definition set that expresses a canon, after Mark's ruling that the five native process definitions are not the trait set. Inventories what exists — five native definitions (four seeded by geometry, only `secrete` acquired), the PD3 pack door that admits authored `ProcessDef`s as data, whole-part seeded mosaics bounded at 64 cells and 8 sites, and a trait board whose `Offer` already carries a `price_mg` but whose candidate list is fed by a fixed two-entry condition table. Reads §7.4's "letters, numbers and symbols" as illustrative rather than ruled (36 / 62 / 94 on three honest readings), sizes on 62, and proposes **60 to 120 definitions** from the ratio rule `N·k/m ≤ T ≤ N·k` with three expressers per glyph and at most four glyphs per trait, cross-checked against the body's own ceiling of roughly 5–30 concurrent traits so mantling stays a choice under scarcity. Structures the catalogue as base, composed and functional entries under one `namespace:name` identity, keeps the rarity ladder's composition depth while refusing its five uncleared tier words in favour of the price the board already charges, makes generated entries `Acquired` by default, and puts generation in `mesocosm-phenotype` rather than the core. Gives a six-step move order whose first two steps hand the expression slice a catalogue larger than five without waiting for a generator. Names the blockers: `ProcessDef::digest` hashes only id, `expressed_by` and `seeding`, so the whole definition space holds **30 distinct rules** and a larger catalogue is names over identical rules unless the definition widens; a pack-minted definition has `native: None` and is read nowhere outside `Registry::of_native`, so it costs cells, earns nothing, and the board's income-minus-rent scorer can never take one; and wing-functions generates Source/Store/Gate/Effect nodes and three operators rather than the bearer/supply/route/gate/sense/effect list the brief assumed, so promoting a blueprint needs an authored `SiteRole` to `(Role, ProcessId)` table and a ruling that abstract charge stays outside the matter ledger. |

## Archive

- `archive_docs/2026-08-07/2026-07-30_body_pipeline_and_host_probe_plan.md`
  — archived 2026-08-07: R0/R1/R3 complete, R2 superseded, R4 transferred
  to the Paredros execution plan (fires after S3).

Retired plans go to `archive_docs/<YYYY-MM-DD>/`.

- [Functional generation](2026-09-09_functional_generation_plan.md): shared charge networks, operators, body bindings, and generator proposal carriage; first slice implemented and tested locally.
