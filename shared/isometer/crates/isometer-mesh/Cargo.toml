[package]
name = "isometer-mesh"
version = "0.0.1"
edition = "2024"
license = "MPL-2.0"
description = "Turns a wing body document into geometry: per-part greedy voxel meshing plus rigid placement. Headless and deterministic, with no GPU dependency."
repository = "https://github.com/merely-made/isocosm"
publish = false

# A projection, not a renderer. It produces quads in integer voxel space; a
# host turns those into vertex buffers, and Isometry's baker turns the same
# quads into sprites. Nothing here may depend on wgpu or a windowing stack.
[dependencies]
# Body documents, part identity, yaw, provenance and the wire seam.
isometer-core = { path = "../isometer-core" }
wing-formats = { path = "../../../wing-formats" }
serde = { version = "1", default-features = false, features = ["derive", "alloc"] }
postcard = { version = "1", default-features = false, features = ["alloc"] }
blake3 = "1.8.5"
# MagicaVoxel ingest, behind the default-off `vox` feature: a consumer that
# only bakes in-crate models pulls no file-format dependency.
dot_vox = { version = "5", optional = true }

[features]
default = []
vox = ["dep:dot_vox"]

[dev-dependencies]
# Fixtures only: `World`, `develop_body`, `Recipe`, `Soma`, `PartPalette` and
# the archetype roster are Mesocosm's development model, which stays with
# Mesocosm. Production code names nothing from it.
mesocosm-core = { path = "../../../../mesocosm/crates/mesocosm-core" }
# The bake lane's appearance recipes round-trip as JSON, and the sprite
# example writes a JSON receipt.
serde_json = "1"
