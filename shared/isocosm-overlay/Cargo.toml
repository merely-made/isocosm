[package]
name = "isocosm-overlay"
version = "0.1.0"
edition = "2024"
license = "MPL-2.0"
description = "The overlay contract: intents, events, views and handoffs a game uses to talk to the Isocosm sim"
repository = "https://github.com/merely-made/isocosm"
publish = false

[workspace]

[dependencies]
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
serde_json = "1"
